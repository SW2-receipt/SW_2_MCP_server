# smithery.yaml

startCommand:
  # 중요: 우린 웹 서버를 띄우니까 http라고 해야 합니다.
  type: http
  command: python
  args:
    - mcp_server.py
  # 서버가 켜지면 어디로 연결할지 알려줍니다.
  url: http://localhost:8000/sse

mcpServers:
  sw2-receipt:
    url: http://localhost:8000/sse
    transport: sse