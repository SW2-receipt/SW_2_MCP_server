FROM python:3.10-slim

WORKDIR /app

# 필수 라이브러리 직접 설치 (파일 의존성 제거)
RUN pip install fastapi uvicorn python-dotenv pydantic mcp

# 파일 복사
COPY mcp_server.py .

# 포트 개방
EXPOSE 8000

# 실행
CMD ["python", "mcp_server.py"]