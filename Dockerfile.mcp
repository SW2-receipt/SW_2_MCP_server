FROM python:3.10-slim

WORKDIR /app

# 1. 스미더리용 가짜 의존성 설치 (pyodbc 없음)
# requirements_mcp.txt가 없으면 그냥 직접 설치합니다.
RUN pip install fastapi uvicorn python-dotenv pydantic mcp

# 2. 파일 복사
COPY . .

# 3. 8000번 포트 열기
EXPOSE 8000

# 4. 실행 (mcp_server.py 실행)
CMD ["python", "mcp_server.py"]